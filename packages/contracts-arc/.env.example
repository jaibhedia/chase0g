# Copy to .env  (gitignored — never commit this file)
#
# Deployer key. On Arc this wallet pays gas in USDC, so fund it first:
#   https://faucet.circle.com
ARC_PRIVATE_KEY=0xyourprivatekeyhere

# Optional: settlement authority, if it should differ from the deployer.
# Leave unset and the deployer becomes the server.
# ARC_SERVER=0x...
